



# P6 完成交接文档

> 写给下一个 AI：本文件记录了 P6 阶段完成的所有工作、架构决策和注意事项，请先通读再开始 P7。

---

## 一、P6 已完成内容

### 1.1 后端

- **`backend/services/score_analyzer.py`（新建，核心）** — 成绩分析 + 课程规划调整业务逻辑
  - `analyze_scores(db, subject_id)`：读学科成绩 → `score_analysis.txt` → LLM → 结构化分析
    - 无成绩抛 400「暂无成绩数据，请先录入成绩」
    - `goal_context`：取最新报告 `content_json.chapters[0]`（「一、当前情况与目标」章），逐级降级为占位串
    - `kb_context` 复用 `report_generator._retrieve_kb_context`（RAG 优雅降级，P8 只换此函数，P6 自动受益）
    - 返回 `{analysis:{trend, trend_detail, weak_points[{area,detail}], gap_analysis, suggestions[], goal_percent}, subject_name, student_name, score_count}`
  - `adjust_plan_preview(db, subject_id)`：**预览两步（不落库）**
    - 先 `analyze_scores`（无成绩自然 400）；取最新 active 规划（无则 400「暂无课程规划，请先生成报告」）
    - `plan_adjustment.txt`（current_plan + score_analysis 文本）→ LLM → `_parse_plan` 清洗 proposed_plan
    - 返回 `{analysis, proposed_plan, reason, source_version}`，**不创建版本**
  - `save_plan_version(db, subject_id, plan_rows, adjustment_reason="")`：`_create_course_plan(..., adjustment_reason=...)` → commit → 返回新 plan（空规划抛 400）
- **`backend/services/report_generator.py`（1 处小改）**：`_create_course_plan` 加可选参数 `adjustment_reason=""`，向后兼容
- **`backend/routers/scores.py`**：
  - `POST /subjects/:id/scores/analyze` → AI 成绩分析
  - `POST /subjects/:id/scores/batch` → 批量录入 `{items:[...]}`（≤200 条；`_to_float` 非数字抛 400；`_normalize_date` 统一 `YYYY-MM-DD`）
  - `create_score`/`update_score` 加校验：`score>=0`、`total_score>0`
- **`backend/routers/course_plans.py`**：
  - `POST /subjects/:id/plans/save` → 手动保存新版本（归档旧 active + 写 adjustment_reason）
  - `POST /subjects/:id/plans/adjust` → AI 调整建议（预览）
  - `POST /subjects/:id/plans`（原 create）改为走 `save_plan_version`，杜绝 version/status 旁路
- **`backend/routers/reports.py`**：`PUT /reports/:id` 新增处理 `course_plan_id`（P6 报告页/规划 Tab 保存新版本后同步报告指向最新规划）
- **Prompt 模板**（Jinja2 StrictUndefined）：
  - `score_analysis.txt`：变量 `student_name, subject_name, scores, goal_context, kb_context`；输出 `{trend, trend_detail, weak_points[], gap_analysis, suggestions[], goal_percent}`
  - `plan_adjustment.txt`：变量 `student_name, subject_name, current_plan, score_analysis, kb_context`；输出 `{reason, plan[]}`

### 1.2 前端

- **`frontend/pages/student_detail.html`（657 行新增，P6 主体）** — 成绩 Tab + 规划 Tab
  - **成绩 Tab（F6.1-F6.5）**：
    - 录入（`Utils.showModal` 单条 + 批量 textarea 每行 `考试名,分数,满分,日期(可省)`，兼容中英文逗号，`parseBatchLines` 报错行带行号）
    - 列表倒序（考试|日期|分数|满分|百分制|备注|✏️/🗑️）
    - **ECharts 曲线**：百分制/原始分切换（`setScoreChartMode` 只 `setOption(..., true)` 不重建）、`goalPercent` 非空时 pct 模式画红色虚线 `markLine` 目标参考线
    - AI 分析面板：趋势徽章 + 薄弱点 + 学习建议 + 与目标差距
    - **ECharts SPA 生命周期**：`let scoreChart`（IIFE 内）+ `disposeScoreChart` + 具名 `onScoreChartResize` + `window.__pageUnmounted`（释放实例 + 移除 resize 监听）
  - **规划 Tab（F6.6-F6.8）**：
    - 可编辑表格（课时|内容|课时数|教师下拉|时间安排|备注|删除行），复用 report.html 的 `data-plan`/`data-i` 模式
    - 统一指派教师下拉（`assignTeacherAllPlan`）
    - 「💾 保存为新版本」：弹窗填调整原因（必填）→ `/plans/save` → 刷新
    - 「🤖 AI 课程调整」：`/plans/adjust` → proposed_plan 载入表格（**未落库**）+ amber 面板展示 reason/趋势 + toast「核对后保存为新版本」
    - 「📜 版本历史」：全部版本（v/日期/当前/归档 + 调整原因 + 预览 + 回退）；**回退 = 复制该版内容追加新版本**（`adjustment_reason: '回退到 vX'`）
    - 空状态：无 active 规划 → 提示先生成报告
- **`frontend/js/app.js`**：`createPageComponent` 加 `unmounted()`，导航离开调用 `window.__pageUnmounted`（ECharts 生命周期关键挂点）
- **`frontend/js/api.js`**：`scores.batch/analyze`、`plans.save/adjust`
- **`frontend/js/utils.js`**：`showModal` 支持 `type:'date'`（浏览器日期选择器）
- **`frontend/pages/report.html`**：保存规划从「原地 update 当前版本」改为「走 `/plans/save` 新建版本 + 弹窗填调整原因（选填）+ 同步 `report.course_plan_id`」——与 P6 规划 Tab 版本纪律一致

### 1.3 其他

- `frontend/index.html`：`APP_VERSION 13→14` + 4 处 `?v=13→?v=14`
- `CLAUDE.md`：进度表 P6 ⬜→✅

---

## 二、关键架构决策

| 决策 | 说明 |
|------|------|
| **AI 调整走"预览两步"** | `/plans/adjust` 只返回建议不落库，用户核对表格后点「保存为新版本」才入库。符合项目铁律"AI 辅助，人工决策"（F6.6 是"调整建议"而非自动改） |
| **版本纪律统一** | 手动保存（规划 Tab/报告页）与 AI 调整保存都走 `/plans/save`（归档旧 active + 新版本 + adjustment_reason）；`POST /plans` 原 create 也收编为同逻辑，杜绝旁路 |
| **回退 = 追加版本** | 不直接翻 status，复制旧版内容生成新版本（reason「回退到 vX」），保持审计链，`report.course_plan_id` 始终指向最新版 |
| **目标参考线** | `goal_percent`（整数 0-100 或 null）由 LLM 从报告「一、当前情况与目标」换算；pct 模式画 markLine，raw 模式不画（各考试满分不同绝对分不可比）；无目标 → null → 不画 |
| **ECharts 生命周期** | SPA 页面反复进出会泄漏/重复实例 → `app.js unmounted` + `window.__pageUnmounted` + `disposeScoreChart` + 具名 resize 监听三件套 |
| **RAG 唯一接入点** | `score_analyzer` 复用 `report_generator._retrieve_kb_context`，P8 实现 ChromaDB 时只改这一处，P6 成绩分析/规划调整自动受益 |
| **日期归一化** | 后端 `_normalize_date` 统一存 `YYYY-MM-DD`，保证成绩列表 `exam_date DESC` 字符串排序正确 |

---

## 三、修改文件清单

```
新增：
├── backend/services/score_analyzer.py     # P6 核心：成绩分析 + 规划调整 + 保存版本
├── build_tailwind.py                      # Tailwind 本地编译脚本（P6后修复，见 §五.13）
├── build/tailwind-input.css               # Tailwind 编译入口（@tailwind 指令）
├── build/tools/                           # Tailwind CLI（npm 安装，构建依赖，不打包）
├── frontend/vendor/tailwind.css           # Tailwind 编译产物（minified ~16KB，本地伺服）
└── P6交接文档.md                           # 本文档

修改：
├── backend/services/report_generator.py   # _create_course_plan 加 adjustment_reason 参数
├── backend/routers/scores.py              # analyze/batch 端点 + 分数校验 + 日期归一化
├── backend/routers/course_plans.py        # save/adjust 端点 + create 收编
├── backend/routers/reports.py             # PUT 支持 course_plan_id
├── backend/ai/prompts/score_analysis.txt  # 重写：结构化分析 + goal_percent
├── backend/ai/prompts/plan_adjustment.txt # 重写：调整规划 JSON + reason
├── frontend/pages/student_detail.html     # 成绩 Tab + 规划 Tab（657 行）
├── frontend/pages/report.html             # 保存规划改走 /plans/save 新建版本
├── frontend/js/app.js                     # createPageComponent unmounted 钩子
├── frontend/js/api.js                     # scores.batch/analyze、plans.save/adjust
├── frontend/js/utils.js                   # showModal 支持 type:'date'
├── frontend/index.html                    # APP_VERSION 14→15；Tailwind CDN 改本地 vendor/tailwind.css
├── CLAUDE.md                              # 进度表 P6 完成 + Tailwind 本地编译纪律
```

---

## 四、接下来 P7 要做什么

按 CLAUDE.md 的 P7 规划（教师管理 + 沟通日志）：

1. **教师管理** — `frontend/pages/teachers.html`（现有占位，路由已有 `#/teachers`，`API.teachers.list/create/update/delete` 已可用）
   - 教师 CRUD 路由已有（`backend/routers/teachers.py`）
   - 表格：姓名、擅长科目（JSON 数组多选）、电话、简介；增删改
   - 课程规划选教师（P6 已接：规划 Tab 教师下拉读 `API.teachers.list`）
2. **沟通日志** — `frontend/pages/student_detail.html` 的 logs Tab 填充（P6 已留占位 `renderLogsTab` 分支）
   - 日志 CRUD 路由已有（`backend/routers/communication_logs.py`，`API.commLogs.*` 已可用）
   - 表单：沟通时间、方式（电话/微信/面谈/其他）、内容摘要；列表倒序 + 编辑/删除
   - 学科详情 Tabs 目前 report/scores/plan 已实现，logs 是最后的占位分支

### P7 已就绪的接口骨架

```
GET/POST /api/teachers、PUT/DELETE /api/teachers/:id
GET/POST /api/subjects/:id/communication-logs、PUT/DELETE /api/communication-logs/:id
```

---

## 五、注意事项（给下一个 AI）

1. **版本号纪律（改前端必做！）**：当前 `APP_VERSION=14`（index.html）+ 4 处 `?v=14`。改前端必须递增 + 同步
2. **页面脚本 IIFE 纪律**：所有页面脚本包 `(function(){...})()` + 末尾 `Object.assign(window,{...})` 导出（必须是 `function` 声明，不能 const 箭头函数）；否则 SPA 重复访问会 `Identifier already declared` 崩溃
3. **ECharts 生命周期**：新建图表页面必须：IIFE 内 `let chart` + `disposeScoreChart` + 具名 resize handler + `window.__pageUnmounted` 注册（app.js unmounted 会调用）；tab 重渲染先 dispose
4. **AI 调用入口**：`ai_manager.get_llm()` / `is_configured('llm')`；未配置时 `_call_llm` 抛 503，前端 toast 提示。成绩分析/规划调整是同步阻塞（几十秒），前端必须 busy 防抖
5. **StrictUndefined 全传**：`score_analysis.txt` 必须传 `student_name/subject_name/scores/goal_context/kb_context`；`plan_adjustment.txt` 必须传 `student_name/subject_name/current_plan/score_analysis/kb_context`。缺一即 500
6. **预览两步纪律**：`/plans/adjust` 不落库；前端务必提示「核对后保存为新版本」，避免用户以为 AI 已保存
7. **版本纪律**：规划任何变更（手动保存/回退/AI 调整保存/报告页保存）都新建版本并归档旧 active；`report.course_plan_id` 指向最新版
8. **RAG 接入点**：P8 实现 ChromaDB 时替换 `report_generator._retrieve_kb_context`，score_analyzer 自动受益
9. **目标参考线**：依赖报告「一、当前情况与目标」章含明确目标分数；无则 `goal_percent=null`（优雅降级）。若用户反馈参考线常缺失，可考虑在对话/报告流程引导采集目标分
10. **测试方法**：后端可用 `score_analyzer` 服务 + 内存库 + mock LLM；真实链路配 DeepSeek（分析 ~7s、调整 ~15s 两次调用）。前端 CDP 驱动见 /tmp/cdp_driver.py 模式
11. **git**：P6 待用户验收后 commit + push；tag 打 `P6-完成`
12. **数据库**：P6 验证的测试数据已清理，恢复 P5 原状（subject4/7 成绩 0 条、规划版本恢复原 active）；如需演示数据可再录
13. **Tailwind 本地化（2026-08-08 修复排版问题）**：`cdn.tailwindcss.com` 在部分网络（大陆）不可用 → 页面仍能渲染但布局全崩（Tailwind utility class 全部失效）。已改为本地编译，离线可用：
    - **重编译纪律**：`python build_tailwind.py` 扫描 `frontend/**/*.{html,js}`，生成 `frontend/vendor/tailwind.css`（构建时编译，minified）。**新增 Tailwind class / 新页面后必须重跑**，否则新 class 静默无样式——这是 Play CDN（运行时编译）改静态编译后的唯一代价
    - CLI 装在 `build/tools/`：`npm install --prefix build/tools tailwindcss@3.4.17 --registry=https://registry.npmmirror.com`（大陆网络快）；也可换 standalone exe 到 `build/tools/tailwindcss.exe`（build_tailwind.py 自动优先用 exe）
    - index.html 引用 `vendor/tailwind.css?v=` 需与 `APP_VERSION` 同步递增
    - 当前 `APP_VERSION=15`

---

## 六、当前进度

```
✅ P1 项目骨架 — 已完成
✅ P2 AI 服务层 — 已完成
✅ P3 学生档案+学科 — 已完成
✅ P4 AI对话采集 — 已完成（2026-08-02）
✅ P5 报告生成导出 — 已完成（2026-08-02）
✅ P6 成绩+规划 — 已完成（2026-08-02）
⬜ P7 教师+日志
⬜ P8 知识库+问答
⬜ P9 工作台+打包
```
