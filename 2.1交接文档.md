# 教培智能体 2.1 开发交接文档

> **给下一位开发者**：本文件是 2.1 的权威交接。先通读本文件 + `docs/2.0开发交接.md`（2.0 全貌）+ `docs/1.0开发事项.md`（1.0 归档，含开发纪律 §七），再动手。**2.1 已完成**：一次全项目审查修复（25 项）+ 用户提出的 7 项新需求。2.1 之后还讨论了「企业级商用化」方向（工程化 / 数据备份强化 / 更新分发），记录在 §六，**均为讨论结论、未实施、待用户拍板**。

**文档版本**：v2.1 交接（2026-08-22）
**当前状态**：2.1 全部完成 — 审查修复 25 项 + 需求 1~7（班型一对几 / 临时调课 / 编辑预填 / 建生跳AI对话 / 课表进班 / 报告建议 / 报告样式）
**前端版本**：APP_VERSION = **50**
**数据规模**：SQLite 16 张表（`class_schedules` 本轮新增 `date` 列，无新表）
**Git**：main 分支，最近提交 `870aa53`（2.1 功能）+ `cbcb7b6`（审查修复），均已推送

---

## 一、2.1 完成内容总览

| 内容 | 提交 | 说明 |
|------|------|------|
| **审查修复 25 项** | `cbcb7b6` | 5 个并行审查代理全项目排查 + 修复：排课自冲突 / 寒暑假班不可编辑 / AI 推理模型协议 / plan 清空 / XSS / 输入校验 / 上传内存 / 看门狗死循环等（详见 §二） |
| **需求1 班型一对几** | `870aa53` | 班型从「一对一/一对多」改为「一对几」（教务手动填"几"），人数上限校验全链路 |
| **需求2 临时调课** | `870aa53` | 课表页「临时调课」：给指定日期手动加一节课（一天可多节，计入总课时流程） |
| **需求3 编辑预填修复** | `870aa53` | 修复 `showModal` 首屏忽略 values 的根本 bug（全部编辑弹窗空白）|
| **需求4 建生跳AI对话** | `870aa53` | 新建学生→新建学科→自动进入该学科 AI 对话采集 |
| **需求5 课表进班** | `870aa53` | 课表课程卡可点击→进入对应班级管理 |
| **需求6 报告建议性** | `870aa53` | 报告课程规划标注「建议性计划」，真实课程在班级管理人工添加 |
| **需求7 报告样式** | `870aa53` | 方法总纲/写在最后红框：修复 pre-wrap 撑高 + 淡红渐变 + 自适应高度 |

**文档索引**：
- `docs/2.0开发交接.md` — 2.0 权威交接（P1~P6 + 三轮优化 + UI 改版 + 排课标准）
- `docs/1.0开发事项.md` — 1.0 完整归档（**开发纪律 §七 必须看**）
- `AI开发交接.md` — AI 工具链 + 硬性纪律 + 候选任务（根目录）
- `backend/tests/regression.py` — 全量回归（63 项，隔离临时库）
- `backend/tests/test_conflicts.py` / `test_scheduler.py` — 排课/冲突单测（15 项）

---

## 二、审查修复轮（`cbcb7b6`，25 项）

**方式**：5 个并行审查代理覆盖全部子系统（AI 服务层 / 路由层 / models+services / 前端 JS / 前端页面）+ 本人逐条复核验证。**未发现 SQL 注入、路径遍历、级联删除等系统性安全问题**。

### 高危（修复了核心路径的致命 bug）
- **排课确认「自己占自己」误判**（`schedules.py` confirm_plan）：冲突检测原把本班旧课次也算进去 → 已有课次的班级再次确认/微调被永久卡死。改为排除本班旧课次，只校验「新方案内部 + vs 其他班占用」
- **寒暑假班排课后不能编辑**（`term_schedule.py` check_term_conflicts）：班期冲突检测不排除自己的课次 → 排过课的暑假班一改就 400。已排除
- **AI 推理模型协议不兼容**（`openai_provider.py`）：o4-mini/gpt-5 需 `max_completion_tokens` 且不支持 `temperature`；deepseek-reasoner 不支持 `temperature` 但仍用 `max_tokens`。已按模型前缀动态组装参数（`_is_openai_reasoning` / `_is_reasoning_model`）
- **报告分节重生成空 plan 清空规划表**（`report_generator.py`）：plan 分支无条件覆盖 `original["plan"]=[]`。已加非空守卫（与「六、」分支一致）
- **确认弹窗存储型 XSS**（`utils.js` confirmDialog + 6 页面）：message 原样拼 HTML，班级/教师/文档名含 `<img onerror=...>` 触发。已改为默认转义 + `messageHtml` 通道

### 中危（健壮性 / 安全性）
- 6 个缺失图标补齐（`icons.html`：edit-3/pause/play/user-check/user-x/info）
- `GET /api/settings` 掩码 api_key（`settings.py`）+ PUT 识别 `****` 回传保留真实 key
- 输入校验缺失 500：新建学科不校验学生存在（`subjects.py`）；班级整数字段归一化 `_to_int`（`classes.py`）；`top_k` 非数字（`knowledge_base.py`）
- 上传先吃满内存 + DB 失败留孤儿文件（`knowledge_base.py` / `document_parser.py`）：改 seek 取大小 + 失败清理
- OpenAI/Claude 超时 60s→120s；报告生成显式 `max_tokens=8192`（`report_generator.py`）
- 班级换教师/教室不同步课次冗余字段（`classes.py` update_class 同步 active 课次）
- `AIManager` 吞 Provider 创建异常 → 补日志（`manager.py`）

### 低危（体验 / 潜在 bug）
- 看门狗无限整页刷新（`app.js`：仅在脚本正常运行时清除 `ta_wdog`）
- 班级详情页任意导航整页 reload（`classes.html`：仅班级路由间切换才 reload）
- api.js 非 JSON 错误响应抛 SyntaxError → 改 text→JSON 兜底
- knowledge_qa 空态选择器写错（`.empty-state`）；knowledge_base 轮询定时器泄漏（`__pageUnmounted` 清理）；settings 全局 error 监听累积
- `sendStudentChat` 快速连发错位（`studentChatBusy` 标志）；AI 目标参考线被重渲染清除；百分制/原始分按钮高亮不一致
- 周视图字符串排序错误（`scheduler.py` 按分钟排序）；方案去重键补 end_time
- 等等（共 25 项，详见 commit message）

---

## 三、需求 1：班型一对几（核心改动）

### 需求
原本新建班级只有「一对一 / 一对多」两个选项 → 改为**「一对几」**，几由教务手动填写（如 一对1、一对2、一对5）。

### 设计（重要，后续排课/班级逻辑都依赖）
- `classes.class_type` 存 `"1vN"` 格式：`1v1` / `1v2` / `1v3` ...；**旧数据 `1vN`（一对多不限人数）兼容保留**
- 辅助函数（`backend/models/class_.py`）：
  - `class_pair_count(class_type) -> int`：`1v1→1`、`1v3→3`、`1vN→0`（0=不限）
  - `class_type_label(class_type) -> str`：`一对N`；`1vN→一对多（小班）`
- **人数上限**（`classes.py`）：
  - 建班：`len(student_ids) > N` → 400
  - 加学生：已有 ≥ N 人 → 400
  - 改班型变小但人数超限 → 400
- `scheduler.py` 优先级：`class_pair_count == 1`（一对1）优先
- `overview.py` 匹配逻辑注释同步（逻辑本身按 subject_id/学科名匹配，未依赖 class_type 值）
- 前端 `classes.html`：班型下拉改为**数字输入「一对几」**（`pair_num`，min=1 必填）；提交转 `1v${n}`；列表/详情/提示显示「一对N」；`student_detail.html` 学科卡同样显示「一对N」
- `seed_demo.py` 演示数据更新为真实一对几（1v1/1v2/1v3）

### 验证
1v3 建 3 人 ✓ / 加第 4 人拒绝 ✓ / 1v1 带 2 人建班拒绝 ✓ / 非法班型 1vX 400 ✓ / 1v3 改 1v2（已有 3 人）拒绝 ✓

---

## 四、需求 2：临时调课（核心改动）

### 需求
课表界面加「临时调课」：可手动给指定日期加一节课（一天可上多节），今天的一节课也算总课时流程中的一节课。

### 设计
- `class_schedules` 新增 **`date` 列**（迁移已登记 `models/__init__.py` MIGRATION_RULES）：
  - `date == ""` → 周循环课（按 weekday 每周重复，原逻辑）
  - `date == "YYYY-MM-DD"` → 临时调课，仅该日期生效一次
- 新端点 **`POST /api/schedules/ad-hoc`**（`schedules.py`）：body `{class_id, date, start_time, end_time, classroom_id, teacher_id}`；日期推导 weekday + 冲突校验（所有 active 课次含本班已有课次，防同班同时段双排）；落库 status=active
- `GET /api/schedules/day`：周循环课排除 `date != ""`；新增 a2 段取 `date == 请求日期` 的临时课，标记 `is_adhoc: true`
- `overview.py _today_classes`：同样纳入临时课（date == 今天）
- 班级详情「上课安排」（`classes.html` loadScheduleList）：临时课显示 `临时 MM-DD` 徽章
- 前端 `schedule.html`：新增「临时调课」按钮 + 弹窗（班级/日期/起止时间/教室/教师）；网格临时课用**琥珀色 + 「临时」徽章**
- 顺带补齐 **end > start 时间区间校验**（`_validate_time_range`，原 add_schedule/update 均缺失 → 倒置区间绕过冲突检测）

### 验证
临时调课落库 ✓ / 当天课表出现 is_adhoc 项 ✓ / 同班同时段二次调课冲突拦截 ✓ / 班级排课列表含临时课 ✓

### 注意
- `schedule_to_weekly` 仍按 weekday 分组，临时课会进对应 weekday 组（仅影响 weekly 接口的展示，schedule 页用的是 /day）
- 临时课计入「总课时流程」= 它是真实课次（进 class_schedules、出现在班级排课列表），**不自动延长寒暑假班 end_date / 不改变 total_lessons**

---

## 五、需求 3~7

### 需求 3：编辑预填原信息（找到根本 bug）
- **根因**：`utils.js` `showModal` 首屏 `render()` 时 DOM 尚空，`collectValues()` 返回 `{}`，但取值回退用了 `f.default` 而非 `initial`（预填的 values）→ **所有编辑弹窗打开都是空白**，用户被迫重新填写
- **修复**：回退改 `initial[f.key]`
- **连带修复**：① select 遇到不在选项里的旧值（如「六年级/新高一」）自动补一个承载原值的选项，防保存丢值；② 年级词汇统一（`Utils.grades` 小六→六年级，与数据库一致）
- **影响面**：全部 showModal 编辑弹窗（学生/班级/成绩/沟通记录/报告章节等）一次性全部修复

### 需求 4：新建学生后自动进 AI 对话
- `student_list.html`：新建学生成功 → 弹「新建学科」→ 选学科确定后 **`location.hash = '#/conversation/' + 学科id`**，自动进入该学科 AI 对话采集（采集完即可生成报告）
- 注意：`showCreateSubjectModal` 在 IIFE 内定义，无需导出

### 需求 5：课表课程卡点击进班级
- `schedule.html` 网格课程卡加 `onclick="location.hash='#/classes/${class_id}'"` + `cursor:pointer` + hover 上浮（`.sched-card`，`style.css`）
- 临时课/固定课/暑假班都能点（都有 class_id）

### 需求 6：报告课程规划只做建议
- 架构本身已分离：报告课程规划是纯数据（`course_plans.plan_json`），**从不创建真实班级/排课**；真实课程在班级管理人工添加
- 在报告「六、本学科落地规划」表格下方加提示：「本规划为**建议性学习计划**（教师/课时仅供 AI 建议参考），具体上课安排请在「班级管理」中创建班级并人工排课」

### 需求 7：报告红框样式
- **根因**：`.report-callout` 容器设了 `white-space: pre-wrap` → 把 HTML 模板缩进换行保留成大量空白行盒，红框被撑高（实测 318px vs 内容仅需 ~140px）
- **修复**：`pre-wrap` 只放到 `.callout-body`；内边距 12px→10px；淡红渐变底 + 16px 正文 + 标题加粗
- 修复后两个红框完全自适应段落长短（方法总纲 140px / 写在最后 83px，均=内容+内边距）
- 章节卡（`.report-section`）无此问题（pre-wrap 本来就在 p 上）

---

## 六、后续方向讨论（已讨论，未实施，待用户拍板）

### 1. 企业级/商用化定位
- 用户目标：**落地商用给培训机构使用**
- 用户判断：当前"不够成熟，像大学生作品"
- 关键结论：**本地单机版是完全可行的商用形态**（大量机构管理系统都是单机版）
- 已给出 P0~P3 分级建议（认证/HTTPS/PostgreSQL/前端工程化/课时财务/监控等），用户看完本地化后又收敛了一版

### 2. 前端工程化重做的必要性（用户专门问了）
- **结论：不是"必须现在做"，但是"越晚做越贵"的债**
- 现状痛点：11 页面全用 innerHTML 字符串拼接 + 全局函数 + 无类型。本次修的 20 个 bug 里一半是"忘转义/状态漏了/变量用错"这类——TS + 组件化在编译期就能挡掉大半
- `executeInlineScripts` / 看门狗 / 版本号自愈 都是"无构建"的补丁机器，上构建工具后整类问题消失
- 推荐**渐进式**：先接 Vite（小改动立得 HMR/缓存指纹）→ 从最高频页面（学生详情/报告/班级）开始组件化 + TS → 补 ESLint
- **决策锚点**：当"加新功能要改的页面数 / 修 bug 耗时"开始让你犹豫做不做这个需求时，就该工程化了

### 3. 数据备份强化（本地商用生死线）
- 现状：启动自动备份 SQLite → `data/backups/` 保留 14 份（`backend/utils/backup.py`）；设置页可手动备份/列表
- 本地商用强化方向（未做）：**一键恢复 / 备份导出到 U 盘网盘 / 换新电脑迁移向导 / 上传文件也纳入备份 / 启动自检**
- 机构数据（学生档案/成绩/财务）全在本地一台电脑，**数据不丢 = 产品生死线**

### 4. 更新分发流程（本地软件）
- 核心优势：**exe 与 data/ 分离** → 更新 = 只换 exe，数据自动保留
- 现在够用的流程：本地打包 → 发 exe 给机构（微信/网盘）→ 机构完全退出程序 → 拖入新 exe 覆盖 → 启动（data 保留）
- **红线**：① 千万别删整个文件夹（data/ 一个都不能动）② 更新前先备份 ③ exe 名带版本号防混淆
- 每次发版必须测「老版本 data → 新 exe 打开」的兼容性（MIGRATION_RULES 自动加列/建表）
- 机构多了之后再做：内置"检查更新"（启动读 version.json）+ 新 exe 自助替换 + 版本登记表

---

## 七、开发纪律（继承 + 新增）

沿用 1.0 §七 / 2.0 硬性纪律，重点重申：

1. **推送规则**：任何修改必须等用户实测验证通过后才 push，除非用户明确说「弄完就提交推送」
2. **改前端必递增 APP_VERSION + 同步 index.html 所有 `?v=`**（当前 50）
3. **新增 Tailwind class / 新页面 → 跑 `build_tailwind.py`**
4. **回归/冲突测试用 `EDU_DATA_DIR` 临时库隔离**，绝不碰 `data/tutoring.db`
5. **`SessionLocal` 是 `autoflush=False`**：先插入再查询前必须显式 `db.flush()`
6. **数据模型铁律**：报告/规划/成绩/日志挂 subjects；删学生级联删全部（PRAGMA foreign_keys 已开）
7. **班型铁律（2.1 新增）**：`class_type` 只允许 `1vN`（N≥1）或旧值 `1vN`；人数上限 = `class_pair_count()`；新增/修改班型逻辑必须走 `_validate_class_type` + `class_pair_count`
8. **临时课铁律（2.1 新增）**：`class_schedules.date` 非空 = 临时课（仅当天）；day 接口周循环段必须排除 `date != ""`；临时课不自动延长寒暑假班班期

---

## 八、验证结果（2.1）

| 项 | 结果 |
|----|------|
| 后端回归 63 项 | ✅ 全通过 |
| 排课/冲突单元测试 15 项 | ✅ 全通过 |
| 定向测试（一对几 5 项 / 临时调课 4 项 / 排课确认自冲突 / 寒暑假班编辑 / 掩码回传 / AI 参数组装） | ✅ 全通过 |
| 无头浏览器实测（CDP + Edge） | ✅ 编辑预填 / 一对几表单 / 临时调课按钮 / 建生弹学科 / 课表点击进班 / 报告样式 全通过 |
| 前端语法检查 | ✅ 全 OK |
| 测试残留清理 | ✅ 已清理（回到 11 学生基线） |

---

## 九、如何开始

```bash
cd E:/教培智能体
venv/Scripts/activate
python main.py            # → 浏览器自动开 http://127.0.0.1:8888
```

1. 先跑一遍 `python main.py` 看 2.1 效果（重点：班级一对几表单、课表临时调课、编辑弹窗预填、报告红框）
2. 读 `测试流程.md` 了解全功能清单
3. 动手前必读 `docs/2.0开发交接.md`（2.0 权威）+ `docs/1.0开发事项.md` §七（纪律）
4. 遇到未拍板的企业级方向（§六），先问用户，不要擅自实施大重构
