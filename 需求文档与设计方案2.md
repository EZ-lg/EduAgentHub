# 教培智能体 — 2.0 需求文档 & 设计方案

---

**项目名称**：教培智能体 — 教培机构一站式学员管理及 AI 助学工具
**文档版本**：V1.0（2.0 需求首稿）
**日期**：2026-08-15
**状态**：待审核
**基线**：1.0 已全部完成（见 `docs/1.0开发事项.md`），本文件在 1.0 需求（`需求文档与设计方案.md`）之上定义 2.0 增量

**更新记录**：
- V1.0：首稿 — 2.0 五大需求 + 部署双轨约束 + P1~P6 开发路线

---

## 目录

1. [2.0 概述与目标](#1-20-概述与目标)
2. [部署形态：EXE ↔ 服务器双轨（架构级约束）](#2-部署形态exe--服务器双轨架构级约束)
3. [核心业务流程（新增）](#3-核心业务流程新增)
4. [功能需求详述](#4-功能需求详述)
5. [数据模型设计（新增/变更）](#5-数据模型设计新增变更)
6. [智能排课引擎设计（核心）](#6-智能排课引擎设计核心)
7. [API 接口设计（新增）](#7-api-接口设计新增)
8. [前端界面设计（新增/改造）](#8-前端界面设计新增改造)
9. [可迁移性设计](#9-可迁移性设计)
10. [开发路线图（P1–P6）](#10-开发路线图p1p6)
11. [1.0 遗留项处理](#11-10-遗留项处理)
12. [风险与待确认项](#12-风险与待确认项)

---

## 1. 2.0 概述与目标

### 1.1 2.0 定位

在 1.0（AI 对话式采集 + 按学科管理，单人单科助学）基础上，**把"个体助学"升级为"机构教务运营"**：

```
1.0：学生 ──学科──> 报告/规划/成绩/日志        （以"人"为单位，一对一助学）
2.0：学生 ──报班──> 班级 ──排课──> 课表        （以"课"为单位，一对多运营）
     + 全局总览把 学生信息/班级/规划 串成一张图
     + 全部设计可迁移到服务器部署为网站
```

一句话：**1.0 解决"单个学生怎么教"，2.0 解决"整个机构怎么排、怎么管、怎么一眼看清"。**

### 1.2 五大需求总览

| 编号 | 需求 | 一句话 |
|------|------|--------|
| G1 | 部署双轨 | exe ↔ 服务器网站双模式，全部设计可迁移 |
| G2 | 班级管理 | 一对一 + 一对多小班，班级/学生可增减 |
| G3 | 智能排课 | 新增班级 → 算法自动排课（教师/教室/学生零冲突）→ 教务确认后落库 |
| G4 | 全局总览 | 学员信息与课程规划串联，一个界面总览 + 钻取 |
| G5 | 学生界面 | 进学生界面先看总体情况一览，点学科/功能再进入细分界面 |

### 1.3 需求确认记录（2026-08-15 已与用户对齐）

| 决策点 | 结论 |
|--------|------|
| 班级与现有数据模型关系 | **班级 = 一门课**（绑学科+教师+教室+时段+学生）；一对一 = 班内 1 人 |
| 智能排课技术路线 | **确定性约束算法 + AI 辅助**（大模型不做排课求解） |
| 服务器迁移深度 | **先抽象不切库**：代码层可迁移化，运行时仍 SQLite 单机，登录暂不做 |
| 学蝉额外功能（签到/消课/统计/财务） | **本次不纳入**（仅面向教务；公网化后再议） |
| 一对多小班学情 | **小班也个性化**：小班学生各自走 AI 对话采集 / 生成个性化报告与规划，上课跟随班级课表 |
| 班级类型 | **学期跟班 / 寒暑假班** 两类：学期班=每周固定 N 次循环；寒暑假班=每天固定 1 节（时段+教室），周日休息，总天数 N |
| 续课 | **默认同时段续课**（追加天数自动顺延）；若续课后该教室时段已被新的排课占用 → 报冲突，提示重新排课 |
| 课表界面 | **日期选择 + 混合解析**：学期班按"周几"展开循环课；寒暑假班按日期在班期内且非周日展开当天课 |

---

## 2. 部署形态：EXE ↔ 服务器双轨（架构级约束）

### 2.1 背景

项目最终形态未定：可能维持单 exe 交付，也可能部署服务器后发布为公网网站。**所有 2.0 开发必须同时兼容两种形态**，不得出现"exe 能用、上服务器就得重写"的设计。

### 2.2 现状盘点（1.0 天然可迁移的部分）

| 层面 | 现状 | 服务器可迁移性 |
|------|------|--------------|
| 前端 | 纯静态 + Hash 路由 + fetch 模板加载 | ✅ 可直接静态托管（Nginx / FastAPI 均支持） |
| 后端 | FastAPI 标准 ASGI 服务 | ✅ 天然服务器化 |
| ORM | SQLAlchemy 2.0 | ✅ 已抽象方言，理论可切 PostgreSQL |
| 静态资源 | 全部本地化到 `frontend/vendor/` | ✅ 无外网依赖，服务器内网也能跑 |
| AI | LLM/Embedding 走 API | ✅ 与部署无关 |

### 2.3 需要 2.0 改造的（详见 §9）

| 层面 | 现状问题 | 改造方向 |
|------|---------|---------|
| 路径 | `config.py` 硬编码 BASE_DIR/data 子目录 | 环境变量化，支持 `DATA_DIR` 覆盖 |
| 数据库方言 | SQLite 专属：`PRAGMA foreign_keys`、`_enable_sqlite_foreign_keys`、JSON 查询、轻量列迁移 | 封装进独立 `backend/utils/db.py`，方言差异收敛于此 |
| ChromaDB | 嵌入式 PersistentClient | 封装 `kb_service`，保留"嵌入式/独立服务"切换口子（本次仍嵌入式） |
| 主机绑定 | `main.py` 写死 `HOST=127.0.0.1` | 环境变量 `HOST/PORT` 可覆盖（服务器绑 0.0.0.0） |
| 认证 | 无（单机单用户） | 本次不做（确认）；服务器化时再引入，架构上留登录拦截中间件空位 |
| 部署 | 只有 PyInstaller spec | 补充服务器部署指南 + 可选 Dockerfile |

> **双轨验收标准**：同一套代码，`python main.py` 跑 exe 形态（自动开浏览器）；`uvicorn backend.app:app --host 0.0.0.0` 跑服务器形态（对外提供服务），两者功能完全一致。

---

## 3. 核心业务流程（新增）

### 3.1 班级管理流程

```
【开新班】
教务 → 班级管理 → 新建班级
  → 选学科（数学/英语…）
  → 选教师（下拉，来自教师管理）
  → 选教室（下拉，来自教室管理；可先建教室）
  → 选班型：一对一 / 一对多
  → 添加学生（一对一默认 1 人；一对多可多选）
  → 保存 → 班级创建成功

【一对一快捷路径】
学生详情 → 该学科 → 「转为一对一班」→ 选教师/教室 → 自动建班
（一对一 = 现有"学生+学科+教师"自动成班，数据无缝衔接）

【调整班级】
班级详情 → 增减学生（增学生时实时校验：该生其它班时间是否冲突）
班级详情 → 改教师/教室/班型/状态（改后需重新排课或手动调整）
```

### 3.2 智能排课流程（确认制）

```
【排课入口】
新建班级完成 / 班级详情 → 「智能排课」

【参数准备】
班级：每周上课次数（如 2 次/周）、时长偏好（如 2 小时）
       → 选择每周固定时段偏好（可选：仅周末 / 晚自习 / 任意）
       → 教室容量已从班级人数自动匹配

【算法求解】← 确定性约束算法（非大模型）
  约束：同教师同时间 ≤1 课
        同教室同时间 ≤1 课
        同学生同时间 ≤1 课（学生可能跨班，全局检查）
        教室容量 ≥ 班级人数
  产出：Top-N 个零冲突候选方案（每个含完整周课表）

【AI 辅助评估】
  对每个候选方案，AI 输出：
   - 教学节奏合理性点评（如"连续三晚都有课，学生负荷偏高"）
   - 教师负荷均衡度
   - 排课瓶颈提示（如"某教师仅剩一个可用时段，建议扩时段"）
  教务参考 AI 建议，挑选一个方案

【确认落库】
教务选择方案 → 点「确认排课」→ 方案写入课表，班级状态更新
（不确认不落库，可关闭重排）
```

### 3.3 全局总览流程

```
教务打开工作台 → 一张图看到：
  ├─ 统计条：在读学生 / 活跃班级 / 本周课次数 / 教师数 / 待确认排课数
  ├─ 学生卡片流：每张卡片 = 学生头像缩写 + 姓名 + 年级
  │     └─ 学科数 / 在学学科标签 / 班级数 / 本周上课数 / 最近报告状态
  │     └─ 点卡片 → 进入学生总览页（见 §4.4）
  └─ 待办区：待确认排课方案 / 今天有课的班级 / 最近活动

学生总览页：
  └─ 顶部：学生信息 + 统计（学科数/班级数/剩余课时[预留]）
  └─ 中部：学科卡片（每科：成绩趋势缩略图 + 报告状态 + 课程规划进度 + 班型/教师）
  └─ 点某学科卡片 → 进入该学科 Tab（报告 / 规划 / 成绩 / 日志）
  └─ 右侧/侧边：该生课表（周视图，来自排课结果）
```

---

## 4. 功能需求详述

> 编号沿用 G 系列。实现情况以验收表勾选。

### G1. 可迁移性（见 §9 详细设计）

| 编号 | 需求 |
|------|------|
| G1.1 | 所有路径配置环境变量化（`DATA_DIR`、`HOST`、`PORT`、`DATABASE_URL`），默认值保持单机行为 |
| G1.2 | SQLite 专属逻辑收敛到 `backend/utils/db.py`，ORM 层不出现方言细节 |
| G1.3 | 部署双模式验证：`python main.py`（exe 形态）+ `uvicorn`（服务器形态） |
| G1.4 | 新增服务器部署指南（含可选 Dockerfile） |
| G1.5 | 登录/多用户本次不做，但预留中间件挂载位（不实现） |

### G2. 班级管理

| 编号 | 需求 | 验收 |
|------|------|------|
| G2.1 | 教室管理 CRUD：名称/容量/位置/备注/状态 | 设置内或独立页可增删改查教室 |
| G2.2 | 班级 CRUD：名称/学科/教师/教室/班型(1v1/1vN)/状态(active/paused)/起止日期 | 班级列表 + 详情 |
| G2.3 | 班级学生增减：一对多可多选学生；一对一固定 1 人 | 增学生时实时冲突校验 |
| G2.4 | 一对一快捷建班：学生详情学科内「转为一对一班」 | 自动生成 1 人班级并关联 |
| G2.5 | 班级状态机：active ⇄ paused（停用保留数据，同学科理念） | 停用后不出现在排课候选 |
| G2.6 | 班级列表页：按状态/学科/教师筛选，显示人数/周课次 | 列表可视化 |
| G2.7 | 学生报班关系可查：学生详情显示其所有班级 | 学生总览联动 |

### G3. 智能排课 + 课表

| 编号 | 需求 | 验收 |
|------|------|------|
| G3.1 | 节次/时间段设置（机构自定义一天 N 个时段，存 settings） | 设置页可配置时段模板 |
| G3.2 | 周课表模型：每周循环固定时段，支持一周多课次 | 数据库可存多段课表 |
| G3.2b | 寒暑假班：每天固定 1 节（时段+教室），周日休息，总天数 N；班期内（跳过周日）该时段+教室不得被占用 | 建班即校验班期冲突 |
| G3.2c | 续课：默认同时段追加天数；若续课范围内该教室时段已被新排课占用 → 报冲突提示重新排课 | 续课 API + 冲突报告 |
| G3.3 | 冲突检测引擎：教师/教室/学生三重冲突实时检测 | 手动排课即时提示冲突 |
| G3.4 | 自动排课算法：确定性约束求解，产出零冲突候选方案 | 一键生成 Top-N 方案 |
| G3.5 | AI 辅助评估：对候选方案点评 + 瓶颈分析 | 方案页显示 AI 建议 |
| G3.6 | 确认制：候选方案待确认 → 教务确认 → 落库；不确认可弃用 | 未确认方案不写库 |
| G3.7 | 全局课表功能栏：周课表（按教师/教室/班级筛选）+ 今日课表 | 课表页可视化 |
| G3.7b | 课表日期解析（混合）：选日期 → 学期班按周几循环课 + 寒暑假班按日期班期内非周日当天课 | `day?date=` 混合解析 |
| G3.8 | 班级排课变更：手动调整课次时冲突实时校验，调整后需确认 | 课表编辑安全 |

### G4. 全局总览

| 编号 | 需求 | 验收 |
|------|------|------|
| G4.1 | 工作台升级为全局总览：统计条 + 学生卡片流 + 待办区 | 一屏看清学员/班级/课表/待确认 |
| G4.2 | 学生卡片聚合：学科数/班级数/本周课次数/最新报告状态/在学标签 | 卡片信息与详情联动 |
| G4.3 | 待办区：待确认排课方案数、今日有课班级、最近活动 | 点待办直达处理 |
| G4.4 | 学生信息与课程规划串联：卡片 → 学生总览 → 学科 → 报告/规划 | 全链路可钻取 |

### G5. 学生界面改造

| 编号 | 需求 | 验收 |
|------|------|------|
| G5.1 | 学生详情改为"总体情况一览"：顶部信息 + 学科卡片区 + 课表 | 一屏总览该生 |
| G5.2 | 点学科卡片 → 进入该学科 Tab（复用报告/规划/成绩/日志） | 保留现有 Tab 功能 |
| G5.3 | 保留快捷入口（新建学科/转一对一/导出报告等） | 操作不增负担 |

---

## 5. 数据模型设计（新增/变更）

### 5.1 总体关系（1.0 + 2.0）

```
classrooms (教室，独立)
     │
classes (班级) ────< class_schedules (班级课次)
     │  subject_id → subjects
     │  teacher_id → teachers
     │  classroom_id → classrooms
     └─────< class_students (班级学生)
                 │ student_id → students
                 └ subject_id → subjects（该生在该班的学科学习记录，复用 1.0 链路）

students (1) ──< subjects (N)        ← 1.0 保留：学习维度（报告/规划/成绩/日志仍挂这里）
students (1) ──< class_students (N)  ← 2.0 新增：上课维度
```

**双维度设计说明**：
- **学习维度（subjects，1.0）**：学生"学什么"的记录仓库，报告/规划/成绩/日志照旧挂在这里，不迁移不破坏。
- **上课维度（classes，2.0）**：机构"怎么上课"的排课单元，一门课一个班。
- 一对一：某生 subjects 记录 + 一个 1 人班级绑定，`class_students.subject_id` 指向该 subjects，双维度打通。
- 一对多：每个学生各有一个对应学科的 subjects 记录，**各自可走 AI 对话采集、生成个性化报告与规划（复用 1.0 能力）**；上课时跟随班级课表。班级是"上课维度"，个性化助学是"学习维度"，两者并存。

> **铁律**：报告、规划、成绩、日志仍挂 `subjects`，**不**因班级引入而迁移。

### 5.2 新增表（4 张，共 12 → 16 张）

| 表 | 关键字段 | 说明 |
|----|---------|------|
| `classrooms` | name / capacity / location / notes / status(active/paused) | 教室资源 |
| `classes` | name / subject_id(FK) / teacher_id(FK) / classroom_id(FK) / class_type("1v1"/"1vN") / weekly_frequency / duration_minutes / start_date / end_date / status(active/paused) / notes | 班级=一门课 |
| `class_students` | class_id(FK) / student_id(FK) / subject_id(FK, 可空) / enroll_date / status(active) | 班级-学生关联，唯一约束(class_id, student_id) |
| `class_schedules` | class_id(FK) / weekday(0-6) / start_time / end_time / classroom_id(FK) / teacher_id(FK) / status(active/archived) | 周循环课次，一周可多行 |

> **待确认**：`class_schedules` 是否需要 `confirmed` 标记（排课确认制）→ 推荐用 status：`draft`(候选未确认) / `active`(已确认) / `archived`(调整归档)，一次排课多行同批确认。

### 5.3 变更的现有表

| 表 | 变更 | 说明 |
|----|------|------|
| `settings` | 新增 key：`class_periods`（节次模板 JSON）、`institution_defaults` 扩展 | 排课时段配置 |
| `subjects` | 不变（可能增加可选 `class_id` 弱关联，待定） | 保持 1.0 结构 |
| `teachers` | 不变 | 已被班级引用 |
| `classes` | 新增 `term_type`(semester/summer_winter)、`total_lessons`(寒暑假班总天数)、`daily_start`/`daily_end`(寒暑假班每天固定时段) | 学期班与寒暑假班双模式 |

### 5.4 迁移策略

- 新增表走 1.0 已有的 `create_all` + 轻量列迁移机制（`backend/models/__init__.py`），旧库自动补表，**无需用户手动操作**。
- 1.0 已有学生/学科/教师数据 100% 保留；已有一对一关系的，用户可自行「转为一对一班」建班，不强迁。

---

## 6. 智能排课引擎设计（核心）

### 6.1 为什么不用大模型排课

| 方案 | 问题 |
|------|------|
| 纯 LLM 生成课表 | 无法保证零冲突（幻觉）、不可复现、输出格式易错、无法证明"没有更优解" |
| **确定性约束算法 + AI 辅助** ✅ | 可证明零冲突、可复现、可控；AI 只做"点评/瓶颈分析"等启发工作 |

### 6.2 输入/输出

```
输入：
  - 班级集合（含教师/教室/学生/每周次数/时长/时段偏好）
  - 教室表（容量）
  - 教师可用时段（可从 settings 或按需维护，v1 取全部时段）
  - 节次模板（settings.class_periods）
  - 已确认课表（已有班级占用的槽位）

输出：
  - Top-N 零冲突候选方案
  - 每个方案 = { class_id → [(weekday, start, end, classroom_id, teacher_id)] }
  - 附冲突报告（空）/ 未满足项说明（如某班次数排不满）
```

### 6.3 约束定义

```
硬约束（必须满足）：
  H1 同一教师同一时段 ≤ 1 课
  H2 同一教室同一时段 ≤ 1 课
  H3 同一学生同一时段 ≤ 1 课（全局跨班检查）
  H4 教室容量 ≥ 班级人数
软约束（尽量满足，用于评分）：
  S1 满足班级时段偏好（周末/晚自习等）
  S2 学生跨班课间 ≥ 30 分钟缓冲
  S3 教师每日上课节数均衡
  S4 教师跨教室距离/时间缓冲
```

### 6.4 算法流程

```
1. 构建槽位网格：weekday × 节次时段（来自 settings.class_periods）× 教室
2. 班级排序（优先级）：一对一 > 一对多；每周次数多 > 少；先到先排
3. 对每个班级，贪心尝试放置 N 次（每次带随机扰动 → 多样性）
4. 冲突回退：当前槽位被占则查下一候选槽位；无可用则标记"未排满"并记录原因
5. 评分：对每个完整候选方案按软约束打分
6. 输出 Top-N 高分方案（确定性结果，可复现）
```

> 实现建议：独立 `backend/services/scheduler.py`（纯函数，不依赖 LLM），单测覆盖约束。

### 6.5 AI 辅助层

- 对 Top-N 方案，逐个调 LLM（prompt：`schedule_eval`），返回：教学节奏点评 / 教师负荷 / 瓶颈提示。
- AI 输出**不参与**冲突判断（算法已保证），仅作决策参考。
- 冷启动：未配 LLM 时跳过 AI 点评，排课功能照常可用（优雅降级，同 RAG）。

### 6.6 确认制实现

- 排课结果存 `class_schedules`，status=`draft`（候选）。
- 教务在课表页查看候选方案（周视图可视化）→ 点「确认」→ 同批 draft → active。
- 弃用：直接删除 draft 行或重新排课覆盖。
- 手动改课：改后即时冲突校验，冲突标红，保存需无冲突。

---

## 7. API 接口设计（新增）

> 统一前缀 `/api/`，响应 `{"success":true,"data":{...}}` / `{"success":false,"error":"..."}`，沿用 1.0 规范。

### 7.1 教室 `/api/classrooms`
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/classrooms` | 教室列表（支持 status 筛选） |
| POST | `/api/classrooms` | 新建教室 |
| PUT | `/api/classrooms/{id}` | 更新教室 |
| DELETE | `/api/classrooms/{id}` | 删除教室（有排课关联时阻止或提示） |

### 7.2 班级 `/api/classes`
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/classes` | 班级列表（筛选：status/subject_id/teacher_id/含人数） |
| POST | `/api/classes` | 新建班级（可选同时带学生/排课参数） |
| GET | `/api/classes/{id}` | 班级详情（学生+课次+教师+教室） |
| PUT | `/api/classes/{id}` | 更新班级（改教师/教室/班型等） |
| DELETE | `/api/classes/{id}` | 删除班级（级联删除关联学生/课次） |
| POST | `/api/classes/{id}/students` | 添加学生（带 conflict 校验返回） |
| DELETE | `/api/classes/{id}/students/{sid}` | 移除学生 |
| POST | `/api/classes/from-subject/{subject_id}` | 一对一快捷建班 |

### 7.3 排课 `/api/schedules`
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/schedules/weekly` | 周课表（筛选：teacher_id/classroom_id/student_id/班级） |
| GET | `/api/schedules/conflicts` | 冲突检测（手动排课预检） |
| POST | `/api/schedules/auto-plan` | 智能排课：返回 Top-N 候选方案 + AI 点评 |
| POST | `/api/schedules/confirm` | 确认某候选方案（draft → active） |
| POST | `/api/schedules` | 手动新增课次（带冲突校验） |
| PUT | `/api/schedules/{id}` | 改课次（带冲突校验） |
| DELETE | `/api/schedules/{id}` | 删课次 |

### 7.4 总览 `/api/overview`
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/overview` | 全局总览聚合（统计条 + 学生卡片流数据 + 待办区） |
| GET | `/api/students/{id}/overview` | 学生总览聚合（学科/班级/课表/报告状态） |

> 学生总览聚合可合并进现有 `students` 路由或独立；建议独立 `overview` 路由，避免撑大旧路由。

---

## 8. 前端界面设计（新增/改造）

### 8.1 路由规划（Hash 模式，沿用）

```
#/dashboard                 → 全局总览（改造）
#/classes                   → 班级列表（新增）
#/classes/:id               → 班级详情（新增：学生/课次/排课/确认）
#/classrooms                → 教室管理（新增，或并入设置）
#/schedule                  → 课表功能栏（新增：周课表/今日课表/排课向导）
#/student/:id               → 学生总览（改造）
#/student/:id/subject/:sid  → 学科详情（保留）
...（conversation/report/teachers/knowledge-base/qa/settings 保留）
```

### 8.2 新增页面

**`classes.html`（班级管理）**
```
├─ 顶部：新建班级按钮 + 筛选（状态/学科/教师/班型）
├─ 班级卡片/列表：名称 / 班型徽章(1v1|1vN) / 学科 / 教师 / 人数 / 周课次 / 状态
└─ 班级详情（内嵌或独立路由）：
     ├─ 信息区：学科/教师/教室/班型/起止日期/状态（可编辑）
     ├─ 学生区：学生列表 + 添加/移除（实时冲突校验提示）
     ├─ 课次区：当前周课次列表 + 「智能排课」入口
     └─ 排课向导（内嵌弹窗）：
          设置周次数/时长/时段偏好 → 生成候选 → 列表展示 Top-N + AI 点评
          → 点选方案 → 周视图预览 → 确认落库
```

**`schedule.html`（课表功能栏）**
```
├─ 日期选择器（默认今天）+ 教师/教室/班级 筛选
├─ 当天课表视图（参考暑假课程表）：
│    行 = 节次时段（第一节~第六节，来自节次模板）
│    列 = 教室（每列一个教室）
│    格 = 课程卡片（班级名/学科 + 教师 + 学生名单），跨时段课显示起止，空教室留白
│    数据 = 混合解析：学期班按该日期周几循环课 + 寒暑假班按班期内非周日当天课
└─ 无排课提示：所选日期无课时显示空态
```

### 8.3 改造页面

**`dashboard.html`（→ 全局总览）**
```
├─ 统计条：在读学生 / 活跃班级 / 本周课次 / 教师数 / 待确认排课数（红色角标）
├─ 学生卡片流（G4.2）：姓名/年级/在学学科标签/班级数/本周课次数/最近报告状态
├─ 待办区（G4.3）：待确认排课方案 / 今日有课班级 / 最近活动
└─ 保留原有趋势图/学科分布（降级为可选区块）
```

**`student_detail.html`（→ 学生总览）**
```
├─ 顶部：头像缩写/姓名/年级/学校/状态 + 统计（学科数/班级数）
├─ 学科卡片区：每科一张卡（学科名/教师/班型/成绩缩略迷你图/报告状态/规划进度）
│    └─ 点卡片 → 进入该学科 Tab（报告/规划/成绩/日志，复用现有）
├─ 班级区：该生所在班级列表 + 周课次
└─ 快捷入口：新建学科 / 转一对一班 / 导出报告
```

### 8.4 Tailwind 纪律

新增/改造页面均使用新 class，**每个 P 完成后必须 `python build_tailwind.py` 重跑**，并按 1.0 纪律递增 `index.html` 的 `APP_VERSION` 与所有 `?v=`。

---

## 9. 可迁移性设计

### 9.1 配置层（`config.py` 改造）

```python
# 优先级：环境变量 > 默认值
DATA_DIR = os.getenv("EDU_DATA_DIR", os.path.join(BASE_DIR, "data"))
DATABASE_URL = os.getenv("EDU_DATABASE_URL", f"sqlite:///{DATA_DIR}/tutoring.db")
HOST = os.getenv("EDU_HOST", "127.0.0.1")     # 服务器部署设 0.0.0.0
PORT = int(os.getenv("EDU_PORT", "8888"))
CHROMA_PATH = os.getenv("EDU_CHROMA_PATH", os.path.join(DATA_DIR, "chroma_data"))
```

### 9.2 数据库方言层（新增 `backend/utils/db.py`）

- 收敛：engine 创建、`PRAGMA foreign_keys`（仅 SQLite 执行）、轻量列迁移、JSON 查询辅助。
- ORM 层一律不出现 `sqlite://` 细节；未来切 PostgreSQL 只改 `db.py` + 环境变量。
- `models/__init__.py` 的 `_enable_sqlite_foreign_keys` 迁移到 `db.py`。

### 9.3 静态资源与部署

- 静态资源已本地化，服务器形态可直接 `uvicorn backend.app:app`（FastAPI StaticFiles 已挂载）或 Nginx 前置。
- 新增 `deploy/` 目录：`Dockerfile` + `nginx.conf` 示例 + `deploy.md`（服务器部署指南）。
- 前端无构建，上传即用（Hash 路由无需服务端 rewrite）。

### 9.4 认证预留（不做实现）

- `app.py` 预留认证中间件注册位（注释标记），未来加登录只需在此挂载 + 前端守卫路由。
- 本次所有接口保持无鉴权（单机/内网语义），服务器化时统一加固。

---

## 10. 开发路线图（P1–P6）

> 每 P 独立可验收，完成后即测即交付。顺序依赖：迁移(1) → 班级模型(2) → 排课引擎(3) → 课表(4) → 总览(5) → 集成(6)。

### P1 可迁移性重构（纯工程，前置）
| 项 | 内容 |
|----|------|
| 目标 | 同一代码双模式可跑，为后续所有 P 打底 |
| 内容 | `config.py` 环境变量化；`db.py` 方言层收敛（PRAGMA/迁移/JSON）；`main.py` HOST/PORT 可配；`deploy/` 文档 + 可选 Dockerfile；双模式冒烟测试脚本 |
| 验收 | `python main.py`（exe 形态）与 `uvicorn`（服务器形态）均正常；单 exe 打包仍可运行 |
| 涉及 | config.py / main.py / backend/app.py / models/__init__.py / backend/utils/db.py(新) / deploy/ |

### P2 教室 + 班级数据模型（需求 G2 主体）
| 项 | 内容 |
|----|------|
| 目标 | 教室/班级/班级学生 三表上线，班级 CRUD + 学生增减 + 一对一快捷建班 |
| 内容 | 新增 3 模型 + 迁移；`/api/classrooms`、`/api/classes` 路由；前端 `classrooms.html`(或并入设置) + `classes.html` 列表/详情；增学生时基础冲突预检（v1 仅提醒，完整检测 P3） |
| 验收 | 班级 CRUD / 学生增减 / 一对一建班 全流程可用；1.0 旧数据无损 |
| 涉及 | models(classrooms/classes/class_students) / routers(classes/classrooms) / frontend(pages/classes.html) |

### P3 智能排课引擎 + AI 辅助（需求 G3 核心）
| 项 | 内容 |
|----|------|
| 目标 | 确定性排课算法 + AI 点评 + 确认制流程 |
| 内容 | `backend/services/scheduler.py`（约束求解，纯函数，单测）；`settings.class_periods` 节次配置；`/api/schedules/auto-plan|confirm`；AI 点评 prompt `schedule_eval.txt` + 优雅降级；候选方案确认落库流程 |
| 验收 | 新增班级一键排课 → Top-N 零冲突方案 → AI 点评 → 确认落库；冲突单测全绿；未配 LLM 时排课可用 |
| 涉及 | services/scheduler.py(新) / ai/prompts/schedule_eval.txt(新) / routers/schedules.py(新) / models/class_schedules |

### P4 课表功能栏（需求 G3 后半）
| 项 | 内容 |
|----|------|
| 目标 | 全局周课表 + 今日课表 + 手动排课冲突检测 |
| 内容 | `schedule.html` 周视图网格（教师/教室/班级/学生筛选）；今日课表；手动增删改课次 + 实时冲突检测（`/api/schedules/conflicts`）；课表编辑确认 |
| 验收 | 课表页可视化完整，冲突实时标红提示，编辑安全 |
| 涉及 | frontend(pages/schedule.html) / routers/schedules.py / services/scheduler.py(检测复用) |

### P5 全局总览 + 学生界面改造（需求 G1/G4/G5）
| 项 | 内容 |
|----|------|
| 目标 | 一屏总览所有学员与课程规划；学生详情改总览式 |
| 内容 | `/api/overview` 聚合 + `/api/students/{id}/overview`；`dashboard.html` 改造为总览（统计条+学生卡片流+待办区）；`student_detail.html` 改造（顶部总览+学科卡片区+班级区+课表）；全链路钻取 |
| 验收 | 工作台一屏看清学员/班级/课表/待确认；学生总览→学科→功能 全链路可钻取 |
| 涉及 | routers/overview.py(新) / frontend(dashboard/student_detail 改造) |

### P6 集成联调 + 双轨验证 + 打包（收尾）
| 项 | 内容 |
|----|------|
| 目标 | 2.0 全功能回归 + 双部署形态验证 + 交付 |
| 内容 | 全量回归（1.0 + 2.0）；冲突场景边界测试（多班争抢教师/教室/学生跨班）；exe 重新打包；服务器模式演练；`docs/2.0开发事项.md` 归档（沿用 1.0 归档套路） |
| 验收 | 双形态全功能一致；exe 交付 + 服务器部署指南可落地；归档完成 |
| 涉及 | 全局 / build / deploy / docs |

### 里程碑
- **M1（P1–P2 后）**：可跑可迁移 + 班级体系上线（含一对一）
- **M2（P3–P4 后）**：智能排课 + 课表闭环，教务可正式使用
- **M3（P5 后）**：总览体验上线，全链路串联
- **M4（P6 后）**：2.0 交付（exe + 服务器双形态）

---

## 11. 1.0 遗留项处理

| 编号 | 内容 | 2.0 处理 |
|------|------|---------|
| F2.7 | 学生 Excel 批量导入 | 不纳入本次，保留 |
| F2.8 | 档案合并导出（全学科 PDF） | 不纳入本次，保留 |
| F5.6 | 课程日历/时间表视图 | **由 G3/G4 课表功能栏天然覆盖**（周课表取代） |
| F11.4 | 报告风格设置 | 不纳入本次，保留 |
| F11.5 | 学科选项管理 | 不纳入本次，保留 |

---

## 12. 风险与待确认项

| # | 风险/待确认 | 影响 | 建议 |
|---|------------|------|------|
| R1 | 班级与 subjects 的双维度对已有用户可能难理解 | 数据模型心智负担 | 文档 + 一对一快捷建班引导 |
| R2 | 一对多班的学生是否需要各自 subjects 报告记录 | 报班学生是否走 AI 采集 | ✅ **已确认**：小班也个性化——各自 AI 采集 + 个性化报告/规划，上课跟随班级课表（2026-08-15） |
| R3 | 排课时段模板 v1 取全部时段，教师可用时段暂不维护 | 排课精确度 | 后续可加"教师可用时段"表，P3 先简化 |
| R4 | 学生跨班冲突全局检查依赖完整排课数据 | 新班排课需已知所有班课表 | 算法基于全量课表求解，天然满足 |
| R5 | 服务器化后的数据备份/并发 | 多用户并发写 SQLite 风险 | 服务器化时评估切 PostgreSQL（本次已留方言层） |
| R6 | 学蝉签到/消课/家长端 | 需求外延 | 明确**本次不做**，公网化后单独立项 |

---

> **2.0 版本状态**：需求首稿待审核。确认后按 §10 P1→P6 顺序开发，每 P 完成后即测即汇报。最终交付：单 exe + 可迁移服务器双形态。
