# 教培智能体 2.0 开发交接文档

> **给下一位开发者**：本文件是 2.0 当前进度的权威交接。先通读本文件 + `需求文档与设计方案2.md` + `docs/1.0开发事项.md`，再动手。**2.0 已完成 P1→P4**，剩 P5（全局总览+学生界面）、P6（联调打包归档）。

**文档版本**：v2.0 交接（2026-08-15）
**当前状态**：2.0 进度 P1~P4 ✅（可迁移性 / 班级教室 / 智能排课 / 寒暑假班+续课 / 课表界面）
**前端版本**：APP_VERSION = **32**
**数据规模**：SQLite 16 张表（1.0 的 12 张 + 2.0 的 4 张）
**Git**：main 分支，最近提交 `630d745`

---

## 一、项目状态总览

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| **M0** | 1.0 全功能版（P1~P9 + 打包） | ✅ 已交付 |
| **P1** | 2.0 可迁移性重构（config 环境变量化 / db 方言层 / deploy 双轨） | ✅ |
| **P2** | 教室 + 班级数据模型（3 新表 + CRUD + 一对一快捷建班） | ✅ |
| **P3** | 智能排课引擎（确定性约束 + AI 点评 + 确认制） | ✅ |
| **P3.5** | 寒暑假班 + 续课（班型双模式 + 班期冲突检测 + 续课） | ✅ |
| **P4** | 课表功能栏（日期选择 + 教室×时段网格 + 混合解析） | ✅ |
| **P5** | 全局总览 + 学生界面改造（工作台总览卡 + 学生总览页） | ⬜ **待做** |
| **P6** | 集成联调 + 双轨打包验证 + 2.0 归档 | ⬜ 待做 |

**文档索引**：
- `需求文档与设计方案2.md` — 2.0 需求源头（含 G1~G5 编号、P 阶段路线）
- `docs/1.0开发事项.md` — 1.0 完整归档（继承的开发纪律必须看 §七）
- `docs/2.0开发交接.md` — 本文件

---

## 二、2.0 需求与已确认决策（用户拍板）

| 决策点 | 结论 |
|--------|------|
| 部署形态 | **exe ↔ 服务器双轨**，代码可迁移（P1 已打地基，运行时仍 SQLite） |
| 班级与数据模型 | **班级=一门课**（绑学科/教师/教室/学生），一对一=班内 1 人 |
| 智能排课 | **确定性约束算法 + AI 辅助**（大模型不做求解） |
| 服务器迁移 | 先抽象不切库，登录暂不做 |
| 学蝉功能 | 签到/消课/统计/财务**本次不纳入**（仅面向教务） |
| 一对多小班 | **小班也个性化**（各自 AI 采集+报告，上课跟班级课表） |
| 班级类型 | **学期跟班 / 寒暑假班**：学期=每周循环 N 次；寒暑假=每天固定 1 节周日休息总天数 N |
| 续课 | **同时段续课优先**，续课后时段被占 → 报冲突提示重排 |
| 课表界面 | 日期选择 + **混合解析**（学期按周几 / 寒暑假按班期日期） |

---

## 三、已完成功能详解

### P1 可迁移性（地基，已提交 `1c69f58`）
- `config.py`：全部路径/URL/端口环境变量化（`EDU_DATA_DIR/DATABASE_URL/CHROMA_PATH/HOST/PORT/FRONTEND_DIR`），默认值保持单机行为
- `backend/utils/db.py`（新）：方言层 `create_app_engine()`（SQLite 专属 connect_args + PRAGMA 外键收敛于此）+ `run_light_migrations()`（轻量列迁移）+ `get_db_type()`
- `models/__init__.py`：只留模型+Session+`MIGRATION_RULES` 迁移规则表
- `deploy/`（新）：Dockerfile + nginx.conf + deploy.md（服务器双轨部署指南）
- **铁律**：ORM 层不出现方言细节；切 PostgreSQL 只改环境变量 + db.py

### P2 教室 + 班级（`036054b`）
- 新表：`classrooms`（教室）、`classes`（班级）、`class_students`（班级学生）
- 路由：`/api/classrooms`（CRUD，删除被班级引用时阻止）、`/api/classes`（CRUD + 学生增减 + 状态机 + 一对一快捷建班 from-subject + 续课 extend）
- 前端：`classes.html`（列表卡片 + 详情：信息/学生/上课安排）+ settings 教室 Tab + 学生详情「一对一」转班按钮
- 班级学科：`subject_name`（学科名，1vN 班级不依赖具体学生记录）+ 可选 `subject_id`

### P3 智能排课（`630d745`）
- 新表：`class_schedules`（课次，status: draft/active/archived）
- `backend/services/scheduler.py`：**确定性约束求解**（纯函数、可复现、不查库）
  - 硬约束：同教师/同教室/同学生 同时段 ≤1；教室容量 ≥ 人数
  - 软约束评分：学生同日课间 ≥30min、教师日负荷 ≤3
  - 班级优先级排序 + 随机扰动（`random.Random(attempt)` 可复现）→ Top-N 方案
- 路由 `/api/schedules`：periods（节次模板）/ weekly / day / auto-plan / confirm / check / 手动 CRUD
- AI 点评：`schedule_eval.txt` prompt，未配 LLM 时优雅降级（返回 null，排课照常）
- 单测：`backend/tests/test_scheduler.py`（7 例，跑 `venv/Scripts/python.exe -m unittest backend.tests.test_scheduler`）
- 前端：班级详情「上课安排」区（智能排课向导：选星期→生成 Top-3→AI 点评→确认落库；手动加课带冲突检测）

### P3.5 寒暑假班 + 续课
- `classes` 新增字段：`term_type`（semester/summer_winter）、`total_lessons`（总天数）、`daily_start/daily_end`（每天固定时段）
- `backend/services/term_schedule.py`（新）：班期日期计算（`compute_end_date` 从 start 起跳过周日取第 N 个上课日）+ `lesson_dates` + `check_term_conflicts`（班期每天 vs 学期班周循环课 + 其他寒暑假班 的教室/教师/学生三重冲突）
- 续课 API `POST /api/classes/{id}/extend`：默认同时段续课；`new_total ≤ 当前` 或续课后班期撞占用 → 返回 `{extended:false, conflicts:[{date,type,message}]}`，**不落库**（事务回滚）
- 前端：班级表单「学期/寒暑假」切换；寒暑假班详情显示班期卡 + 续课按钮 + 冲突弹窗

### P4 课表功能栏
- `schedule.html`（新页，导航「课表」）：**日期选择 + 教室×时段网格**（参考用户给的暑假课程表图：行=节次时段、列=教室、格=课程卡含教师/学生，空教室留白）
- `GET /api/schedules/day?date=`：**混合解析**——学期班 active 课次按该日期 weekday 展开 + 寒暑假班按日期在班期内且非周日展开
- 教师筛选、今天按钮
- 班级列表顶部「全部/寒暑假班/学期跟班」筛选按钮组 + 卡片模式徽章

---

## 四、数据模型全貌（16 张表）

```
1.0（12 张，保留）：
  students / subjects / ai_conversations / reports / scores / course_plans
  / communication_logs / teachers / knowledge_docs / qa_history / settings / activity_logs

2.0（4 张新增）：
  classrooms   教室：name/capacity/location/status
  classes      班级：name/subject_id/subject_name/teacher_id/classroom_id
              /class_type(1v1|1vN)/term_type(semester|summer_winter)
              /total_lessons/daily_start/daily_end/weekly_frequency
              /duration_minutes/start_date/end_date/status/notes
  class_students 班级学生：class_id/student_id/subject_id(可空)/enroll_date，唯一约束(class_id,student_id)
  class_schedules 课次：class_id/weekday(0周一~6周日)/start_time/end_time
              /classroom_id/teacher_id/status(draft|active|archived)
```

**双维度设计**（2.0 核心心智）：
- **学习维度**（1.0 `subjects`）：学生"学什么"，报告/规划/成绩/日志挂这里，**不迁移**
- **上课维度**（2.0 `classes`）：机构"怎么上课"，绑定教师/教室/学生/排课
- 一对一：`class_students.subject_id` 指向学生的 subjects 记录，双维度打通
- **铁律**：报告/规划/成绩/日志仍挂 `subjects`，不因班级引入而迁移

**迁移机制**：新列通过 `models/__init__.py` 的 `MIGRATION_RULES`（`run_light_migrations`）自动补列。**新增列时在这里登记**。

---

## 五、关键技术点与踩坑记录（⚠️ 必须知道）

1. **排课不用大模型**：LLM 无法保证零冲突（幻觉/不可复现）。排课=确定性算法，AI 只做点评。改排课逻辑只动 `scheduler.py`。
2. **`autoflush=False` 坑（已踩）**：`SessionLocal` 是 `autocommit=False, autoflush=False`。新建对象（如 ClassStudent）**不会自动 flush 到查询可见**。建寒暑假班冲突校验前必须**显式 `db.flush()`**，否则学生冲突漏检。任何"先插入再查"的场景都要注意。
3. **寒暑假班 vs 学期班冲突**：`check_term_conflicts` 同时检查学期班 active 课次（按 weekday）和其他寒暑假班（按班期日期）。续课用"模拟更新 → 校验 → 冲突回滚"模式，保证不落库。
4. **混合日期解析**（`/api/schedules/day`）：学期班=周循环（weekday 匹配），寒暑假班=班期内非周日（周日休息）。两者可能同日共存（已验证）。
5. **前端 IIFE 纪律**：页面脚本包 `(function(){...})()` + 末尾 `Object.assign(window, {...})` 导出（必须 `function` 声明）。所有 `onclick` 引用的函数必须导出，否则点击无反应（曾因 `currentClassId` 未全局暴露导致按钮失效）。
6. **前端模板缓存**：页面通过 `fetch('/pages/xxx.html?v=VER')` 加载并缓存 `templateCache`。**改页面模板后必须递增 `index.html` 的 `APP_VERSION` 和所有 `?v=`**，否则用户看到旧模板。
7. **Tailwind 纪律**：新增 Tailwind class / 新页面后必须 `venv/Scripts/python.exe build_tailwind.py` 重跑，否则新 class 静默无样式。自定义 CSS（如 `.term-btn`）加到 `frontend/css/style.css`。
8. **路由参数获取**：页面通过 `location.hash.split('/')` 解析（如 `#/classes/5` → classId=5），不用 Vue props。
9. **API 错误格式**：`HTTPException(400)` 返回 `{"detail": ...}`（**无 success 键**）；`success_response` 返回 `{"success": true, "data": ...}`。前端 `API.request` 对非 2xx 会 throw。业务级"失败"尽量用 `success_response({...ok:false, message, conflicts})` 而非 HTTPException，前端体验更统一。
10. **showToast 只接受 2 参数**（message, type），固定 3 秒，无 duration 参数。
11. **ECharts 生命周期三件套**（P5 会用）：IIFE 内 `let chart` + dispose + 具名 resize handler + `window.__pageUnmounted` 注册。

---

## 六、当前代码结构（2.0 新增/改动重点）

```
backend/
├── utils/db.py                  # 方言层（P1 新）
├── models/
│   ├── classroom.py / class_.py(类名Class) / class_student.py / class_schedule.py  # 2.0 四模型
│   └── __init__.py              # MIGRATION_RULES 迁移登记
├── routers/
│   ├── classrooms.py            # /api/classrooms
│   ├── classes.py               # /api/classes（含 extend 续课）
│   └── schedules.py             # /api/schedules（含 day 混合解析）
├── services/
│   ├── scheduler.py             # 排课算法（纯函数+单测）
│   └── term_schedule.py         # 寒暑假班日期+冲突
├── ai/prompts/schedule_eval.txt # 排课 AI 点评
└── tests/test_scheduler.py      # 排课单测（7 例）

frontend/
├── pages/classes.html           # 班级管理（列表+详情+排课向导+续课）
├── pages/schedule.html          # 课表功能栏（P4 新）
├── js/api.js                    # + classrooms/classes/schedules API
├── js/app.js                    # + /classes /schedule 路由
├── components/navbar.html       # + 班级管理/课表 导航
└── css/style.css                # + .term-btn 等
```

---

## 七、待办事项（P5/P6 详细规划）

### P5 全局总览 + 学生界面改造（下一步）
需求见 `需求文档与设计方案2.md` §4 G4/G5、§8.3。要点：
- **工作台升级为全局总览**：统计条（在读/活跃班级/本周课次/待确认排课数）+ **学生卡片流**（每卡聚合：学科数/班级数/本周课次数/最近报告状态/在学标签）+ 待办区（待确认排课/今日有课/最近活动）
- **学生详情改造**：顶部总览（信息+学科数+班级数）+ 学科卡片区（成绩缩略迷你图/报告状态/规划进度）+ 班级区（所在班+课次）+ 全链路钻取（学生→学科→报告/规划/成绩）
- 新增聚合 API：`/api/overview` + `/api/students/{id}/overview`
- 提示：dashboard.html 是 1.0 工作台，改造时保留原有趋势图可选区块；学生详情参考 P4 的 `schedule.html` 网格风格做"总览+入口"

### P6 集成联调 + 双轨打包 + 归档
- 全量回归（1.0 + 2.0）；冲突边界测试（多班争教师/教室/学生跨班）
- exe 重新打包（PyInstaller，`venv/Scripts/python.exe -m PyInstaller build/build.spec --noconfirm`）
- 服务器模式演练（uvicorn 0.0.0.0）
- 写 `docs/2.0开发事项.md` 归档（沿用 1.0 归档套路）

### 已知遗留/候选
- 教师可用时段表（排课精度提升，当前 v1 取全部时段）
- 课表按周视图（P4 是单日网格，周视图可在 P6 后加）
- 学生 Excel 导入 / 档案合并导出 / 报告风格设置（1.0 遗留 F 项，未纳入 2.0）

---

## 八、开发纪律清单（每步必守）

1. **改前端后递增 APP_VERSION + 同步 `?v=`**（当前 32）
2. **新增 Tailwind class / 新页 → `build_tailwind.py` 重跑**
3. **IIFE + Object.assign 导出**（function 声明）
4. **`MIGRATION_RULES` 登记新列**
5. **`db.flush()` 显式调用**（autoflush=False 场景）
6. **排课只动 scheduler.py，不用 LLM 求解**
7. 数据模型铁律：报告/规划/成绩/日志挂 `subjects`
8. 测试方法：后端用 `-m unittest backend.tests.test_scheduler`；真实链路配 DeepSeek 验证；前端浏览器 F12 Console 看报错

---

## 九、当前数据 / 样例说明

数据库 `data/tutoring.db` 当前包含**演示样例数据**（用户测试用，非真实业务）：
- **7 间教室**（参考图）：评估室/VIP1/VIP2/VIP3/4号课堂/5号课堂(物理室)/6号课堂(休息室)
- **8 名学生**：1.0 的 4 个 + 2.0 新增 王小明/赵乐乐/孙悦/周航
- **6 个班级**：
  - 学期班：初二数学冲刺班(VIP1,每周2次已确认)、高三英语一对一(VIP2,每周2次已确认)
  - 寒暑假班：新初三数学班(4号课堂,每天14:00,**40天**结束8/20)、新高二物理班(5号课堂,每天16:10,12天)、小升初英语班(VIP3,每天8:00,10天)、**新高一语文班(4号课堂,每天14:00,8/21起,10天——这是"冲突占位班"，专门演示续课冲突)**
- **演示场景**：新初三数学班续课到 41 天会撞「新高一语文班」(8/21 教室冲突) → 可验证冲突弹窗

> 这些是测试数据，用户可能后续要求清理。**不要在生产使用前依赖它们**。清理方法：班级管理删班级 → 设置-教室管理删教室 → 学生档案删学生。

---

## 十、给下一位开发者的特别提醒

1. **先读三个文档**（本文件 + 需求2 + 1.0开发事项），再读代码。项目已有良好注释，代码结构与文档 §六 对应。
2. **用户是产品经理型老板**，会实际点界面验证。每完成一个 P 阶段：**先自己端到端测**（API + 前端逻辑），再让用户实测，**用户确认后才提交推送**。
3. **沟通风格**：用中文，直接给方案和推荐，重要分叉用 AskUserQuestion 让用户拍板，别自作主张重构数据模型。
4. **双轨约束常记心**：任何新功能都要考虑"exe 能跑 + 服务器也能跑"，别引入方言专属写法。
5. **排课/续课/冲突检测是 2.0 的价值核心**，改这块务必跑单测 + 构造冲突场景验证（学生跨班、教室被占、教师满负荷）。
6. 样例数据里的「新高一语文班」是演示占位班，测试冲突后可删，但不影响功能。

---

**交接状态**：2.0 进行到 P4 全部完成并推送（`630d745`），P5 是下一步。祝你顺利！🚀
