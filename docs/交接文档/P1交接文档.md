# P1 完成交接文档

> 写给下一个 AI：本文件记录了 P1 阶段完成的所有工作、架构决策和注意事项，请先通读再开始 P2。

---

## 一、P1 已完成内容

### 1.1 后端

- **main.py** — 入口，自动查找可用端口（8888起），启动 uvicorn + 自动打开浏览器
- **config.py** — 全局配置：`BASE_DIR`、`DATA_DIR`、`DB_PATH`、`CHROMA_PATH`、`UPLOAD_DIR`、`EXPORT_DIR`、`DATABASE_URL`。区分开发模式和 PyInstaller 打包模式
- **backend/app.py** — FastAPI 应用工厂，注册全部路由，挂载前端静态文件，startup 事件自动建表
- **backend/models/** — 11 个 SQLAlchemy 模型（完全对应需求文档 §5.2）：
  `Student`, `Subject`, `AIConversation`, `Report`, `Score`, `CoursePlan`, `CommunicationLog`, `Teacher`, `KnowledgeDoc`, `Setting`, `ActivityLog`
  每个模型都有 `to_dict()` 方法。外键级联删除已配置（学生→学科→关联数据）
- **backend/routers/** — 11 个路由文件，学生和学科已实现完整 CRUD，其余为骨架返回占位信息
- **backend/utils/helpers.py** — `now_iso()`、`success_response()`、`error_response()`

### 1.2 前端

- **CDN 方案**：Vue 3 + Vue Router 4 + Tailwind CSS + ECharts 5，零构建
- **frontend/index.html** — SPA 入口，加载 CDN + 挂载点
- **frontend/js/app.js** — Vue 应用核心：`reactive` 全局状态 + `provide/inject` + Hash 路由 + `fetch()` 动态加载页面模板
- **frontend/js/api.js** — `window.API` 封装所有后端接口，统一 JSON 解析和错误处理
- **frontend/js/utils.js** — `window.Utils`：日期格式化、学科图标映射、状态标签、防抖等
- **frontend/css/style.css** — 白色简洁风格，面板/按钮/表格/输入框/统计卡片等组件样式
- **frontend/components/navbar.html** — 侧边栏：上半 4 个主导航，底部固定「系统设置」+ 版本号
- **frontend/pages/** — 9 个页面：dashboard（已实现统计+快速开始）、student_list（列表+搜索+分页+CRUD 弹窗）、student_detail（信息+学科卡片+Tabs），其余 6 个占位

### 1.3 开发环境

- Python 虚拟环境：`venv/`（已在 .gitignore）
- 依赖通过清华源安装：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`
- 启动：`venv\Scripts\python main.py` 或 `source venv/Scripts/activate && python main.py`

---

## 二、关键架构决策

| 决策 | 说明 |
|------|------|
| API 响应格式 | 统一 `{"success": true, "data": {...}}` / `{"success": false, "error": "..."}` |
| 时间格式 | 所有时间戳用 ISO 字符串存储（`datetime.now().isoformat()`） |
| 数据库路径 | `data/tutoring.db`，相对 config.py 的 `BASE_DIR` |
| 路由参数 | FastAPI 接口用 `data: dict` 接收 JSON body（非 Pydantic 模型），适合快速原型 |
| 前端模板 | 页面 HTML 通过 `fetch('/pages/xxx.html')` 加载，缓存到 `templateCache` |
| 前端路由 | Hash 模式 `#/dashboard`、`#/students`、`#/student/:id`、`#/conversation/:id` 等 |
| 侧边栏实现 | 原始 HTML + 原生 JS 高亮（非 Vue 组件），因 navbar 在 Vue app 挂载前加载 |
| 数据关系 | reports / scores / course_plans / communication_logs 全部挂在 subjects 下 |

---

## 三、当前项目文件结构

```
E:\教培智能体\
├── main.py
├── config.py
├── requirements.txt
├── CLAUDE.md                    ← 项目开发指南（读这个！）
├── 需求文档与设计方案.md          ← 完整需求（读这个！）
├── P1交接文档.md                 ← 本文件
├── venv/                        ← Python虚拟环境（已gitignore）
├── .gitignore
│
├── backend/
│   ├── __init__.py
│   ├── app.py                   ← FastAPI 应用工厂
│   ├── models/ (11个模型)
│   ├── routers/ (11个路由)
│   ├── utils/helpers.py
│   ├── ai/                      ← 目录已创建但为空（P2实现）
│   └── services/                ← 目录未创建（P4+实现）
│
├── frontend/
│   ├── index.html
│   ├── css/style.css
│   ├── js/ (app.js, api.js, utils.js)
│   ├── components/navbar.html
│   ├── pages/ (9个页面)
│   └── vendor/                  ← 目录未创建（P9打包用）
│
└── data/
    ├── tutoring.db              ← SQLite自动生成（已gitignore）
    ├── chroma_data/             ← ChromaDB自动生成（已gitignore）
    ├── uploads/.gitkeep
    └── exports/.gitkeep
```

---

## 四、接下来 P2 要做什么

按 CLAUDE.md 的 P2 规划，需要实现：

1. **BaseLLMProvider 抽象类** — `backend/ai/base.py`
   - `chat(messages) -> str`
   - `chat_stream(messages) -> Generator`
   - `embed(texts) -> List[float]`
   - `health_check() -> bool`

2. **至少 2 个 Provider 实现** — `backend/ai/providers/`
   - `openai_provider.py`
   - `deepseek_provider.py`（或 claude/qwen）
   - 每个 Provider 读取自己的 config dict 初始化

3. **AIManager 单例** — `backend/ai/manager.py`
   - 从 settings 表读取 `llm_config` 和 `embedding_config`
   - 根据 provider 字段创建对应 Provider 实例
   - `reload_config()` 切换模型

4. **AI Factory** — `backend/ai/factory.py`
   - 根据 provider 名称返回对应的 Provider 类

5. **系统设置页 AI 配置** — `frontend/pages/settings.html`
   - LLM 配置表单：provider 下拉、API Key、Base URL、模型名、温度、最大 Token
   - Embedding 配置表单：同上
   - 「测试连接」按钮 → 调用 `/api/settings/test-llm` 和 `/api/settings/test-embed`

6. **后端设置接口完善** — `backend/routers/settings.py`
   - test-llm：读取配置 → 创建临时 Provider → health_check → 返回结果
   - test-embed：同上

---

## 五、注意事项（给下一个 AI）

1. **不要动前端风格**：用户经过多轮调整才满意当前的白色简洁风格，侧边栏布局（4 个主导航在上 + 系统设置固定底部）不要再改
2. **运行前先激活 venv**：`source venv/Scripts/activate`（Git Bash）或 `venv\Scripts\activate`（CMD）
3. **测试 API**：用 `curl http://127.0.0.1:8888/api/xxx` 或浏览器直接访问
4. **数据库自动创建**：首次运行 `python main.py` 会自动建表和 data 目录
5. **前端调试**：浏览器 F12 看 Network 和 Console，页面模板加载失败会显示"加载失败"提示
6. **Settings 表是 key-value**：key 是字符串，value_json 存 JSON 字符串。LLM 配置 key 为 `llm_config`，Embedding 为 `embedding_config`
7. **已有代码风格**：路由用 `data: dict` 接收 body（非 Pydantic），响应用 `success_response()` / `error_response()`，时间用 ISO 字符串
8. **ChromaDB 版本**：0.5.x，嵌入式模式，数据目录 `data/chroma_data/`（P8 才用到）
9. **git 仓库**：已关联 GitHub `EZ-lg/EduAgentHub.git`，每个阶段完成后 commit + push
10. **P2 写完 Provider 后建议立刻试打包验证 ChromaDB 兼容性**（见 CLAUDE.md 风险表）

---

## 六、当前进度

```
✅ P1 项目骨架 — 已完成（2026-07-23）
⬜ P2 AI 服务层 — 接下来开始
⬜ P3 学生档案+学科
⬜ P4 AI对话采集
⬜ P5 报告生成导出
⬜ P6 成绩+规划
⬜ P7 教师+日志
⬜ P8 知识库+问答
⬜ P9 工作台+打包
```
