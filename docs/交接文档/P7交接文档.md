# P7 完成交接文档

> 写给下一个 AI：本文件记录了 P7 阶段完成的所有工作、架构决策和注意事项，请先通读再开始 P8。

---

## 一、P7 已完成内容

### 1.1 前端

- **`frontend/pages/teachers.html`（重写占位页）— 教师管理**
  - 列表表格：姓名 | 擅长科目（chip 标签）| 电话 | 简介 | 操作（✏️/🗑️）
  - 顶部搜索框：`searchTeachers` → `API.teachers.list(kw)`（后端对 name/subjects 做 contains 过滤）
  - 新增/编辑弹窗：`showAddTeacherModal(id)` 两列填表式（姓名必填 + 电话 + 擅长科目多选 + 简介）
  - `subjects` 为 JSON 数组字符串：表单结果 `JSON.stringify(data.subjects)` 存储，展示时 `parseSubjects()` 解析为 chip
  - 删除：`confirmDialog` 确认，提示"历史课程规划中已指派的该教师将变为无主（数据保留）"
  - 空状态引导 + 教师人数计数 + `window.currentPageRefresh = loadTeachers`

- **`frontend/pages/student_detail.html` — 沟通日志 Tab（替换占位）**
  - `renderLogsTab()` / `buildLogsHtml()`：按 `log_time` 倒序的纵向列表
  - 每条：方式徽章（电话=蓝 / 微信=绿 / 面谈=紫 / 其他=灰）+ 日期时间 + 内容（`white-space:pre-wrap`）+ 编辑/删除
  - `showAddLogModal(editId)`：沟通时间（`type:'date'`，默认今天）+ 沟通方式（select）+ 内容摘要（textarea 必填）
  - 空状态引导；`switchSubjectTab` 加 `logs` 分支；`Object.assign` 导出 `renderLogsTab/showAddLogModal/editLog/deleteLog`

- **`frontend/js/utils.js` — `showModal` 新增 `multi-select` 字段类型**
  - 渲染 checkbox 组，收集结果为字符串数组；`required` 校验兼容空数组
  - 用法：`{ key, label, type: 'multi-select', options: [{v,label}] }`
  - 纯增量扩展，其他字段类型完全不变（沿用 P6 加 `date` 类型的先例）

- **`frontend/index.html`**：`APP_VERSION 15→16` + 4 处 `?v=16`；`build_tailwind.py` 重跑（新增 `bg-blue-50/bg-green-50/bg-purple-50/text-3xl` 等 class）

### 1.2 后端

- **零改动**：`backend/routers/teachers.py`、`backend/routers/communication_logs.py` 在早期骨架已就绪，P7 直接复用
- 核对确认字段：
  - `Teacher`：`name/phone/subjects(JSON数组字符串)/intro`
  - `CommunicationLog`：`subject_id(挂学科，不挂学生)/method/content/log_time`

### 1.3 其他

- `CLAUDE.md` 进度表：P7 ⬜→✅

---

## 二、关键架构决策

| 决策 | 说明 |
|------|------|
| **擅长科目用 multi-select 而非手填** | 扩展 `showModal` 加 `multi-select` 类型（checkbox 组），收集结果为数组；与骨架 `Teacher.subjects`（JSON 数组字符串）一致。选项为常用学科预设，含"其他"兜底 |
| **沟通日志挂 subject 不挂 student** | 沿用骨架 `communication_logs.subject_id` FK 设计，与报告/成绩/规划一致（CLAUDE.md 数据模型铁律：一切业务数据挂 subjects） |
| **log_time 只取日期粒度** | date 选择器 → `YYYY-MM-DD`，排序用字符串倒序即可；沟通日志的"时间"到天足够，如需时分后续再加 |
| **删除教师保留历史引用** | 教师删除不级联改动课程规划；规划表里的教师字段会显示为无主（历史数据完整，符合"停用≠删除"理念） |
| **搜索走后端** | `API.teachers.list(search)` 复用后端 name/subjects contains，前端不本地过滤，为将来大数据量留余地 |

---

## 三、修改文件清单

```
新增：
├── P7交接文档.md                           # 本文档

修改：
├── frontend/pages/teachers.html           # 占位 → 完整教师 CRUD + 多选 + 搜索
├── frontend/pages/student_detail.html     # logs Tab 填充 + switchSubjectTab 分支 + 导出
├── frontend/js/utils.js                   # showModal 新增 multi-select 字段类型
├── frontend/index.html                    # APP_VERSION 15→16 + 4 处 ?v=
├── frontend/vendor/tailwind.css           # 重编译（新增方式徽章配色等 class）
└── CLAUDE.md                              # 进度表 P7 完成
```

---

## 四、接下来 P8 要做什么

按 CLAUDE.md 的 P8 规划（知识库 + 智能问答）：

1. **后端**：
   - `backend/services/kb_service.py`（新建）：ChromaDB 0.5 嵌入式初始化（`data/chroma_data/`）+ 文档切片入库 + 语义检索
   - `backend/services/document_parser.py`：python-docx / PyPDF2 / markdown 解析上传文档
   - `backend/routers/knowledge_base.py`（骨架已有，填充）：上传/列表/删除/启用停用 + `/api/knowledge/qa` 问答 + presets
   - **RAG 唯一接入点**：替换 `report_generator._retrieve_kb_context`（P6 已注明，score_analyzer 自动受益）
2. **前端**：
   - `frontend/pages/knowledge_base.html`（占位）：上传/管理界面（分类、状态）
   - `frontend/pages/knowledge_qa.html`（占位）：智能问答 UI + presets
   - `api.js` 的 `knowledgeBase.*` 已封装就绪
3. **Prompt**：`knowledge_qa.txt` 模板已存在，检查变量/StrictUndefined

### P8 已就绪的接口骨架

```
GET /api/knowledge-docs?category=  / POST /api/knowledge-docs/upload
GET/PUT/DELETE /api/knowledge-docs/:id（含 /status）
POST /api/knowledge/qa    GET /api/knowledge/qa/presets
```

---

## 五、注意事项（给下一个 AI）

1. **版本号纪律（改前端必做！）**：当前 `APP_VERSION=19`（index.html）+ 4 处 `?v=19`。改前端必须递增 + 同步
2. **Tailwind 重编译纪律**：新增 Tailwind class / 新页面后必须 `python build_tailwind.py`（否则新 class 静默无样式）。详见 P6交接文档.md §五.13
3. **IIFE 纪律**：页面脚本包 `(function(){...})()` + 末尾 `Object.assign(window,{...})` 导出（必须是 `function` 声明）；`window.currentPageRefresh` 注册刷新
4. **multi-select 用法**：`showModal` 的 `{ key, type:'multi-select', options:[{v,label}] }` 收集结果为字符串数组；入库前要 `JSON.stringify`，展示要解析回数组
5. **沟通日志 log_time**：目前只存 `YYYY-MM-DD`（date 选择器），后端 `now_iso()` 兜底；如需时分粒度后续扩展
6. **AI 调用入口**：`ai_manager.get_llm()`；未配置抛 503，前端 toast。P7 无 AI 调用
7. **RAG 接入点**：P8 实现 ChromaDB 时替换 `report_generator._retrieve_kb_context`，score_analyzer 自动受益
8. **测试方法**：后端用 curl + 临时 JSON 文件（Windows 下 shell 直接传中文 JSON 易编码错误，用 `--data-binary @file`）；前端 CDP 驱动见 /tmp/cdp_driver.py 模式
9. **git**：P7 待用户验收后 commit + push；tag 打 `P7-完成`
10. **数据库**：P7 测试数据已清理（测试教师/日志已删，恢复种子原状：4 位教师、0 沟通日志）
11. **Tab 跳转 bug 已修（2026-08-08，v16→17）**：学科详情四 Tab（报告/规划/成绩/日志）切走后，**切回窗口/标签页或刷新会自动跳回「报告」**。根因：app.js 的 `visibilitychange` 和 6s 加载看门狗都会调 `currentPageRefresh` → `refreshStudentDetail` 原硬编码 `renderReportTab()`。已改为 `currentSubjectTab` 记忆 + `renderSubjectTab` 分发器，刷新保留当前 Tab。**后续加 Tab 时务必走 `renderSubjectTab` 分发，不要直接 `renderReportTab()`**
12. **规划 Tab 指派老师/ AI 调整 bug 已修（v17→18）**：`drawPlanTab` 的快照/恢复机制会把旧 DOM 值盖回程序新改的值。**「统一指派教师」选了但每行仍是未指派** 根因：`assignTeacherAllPlan` 改完 `planRows` 后调 `drawPlanTab`，快照抓到旧下拉值并恢复回去。AI 调整 `runPlanAdjust` 同理会盖掉 AI 建议。已修：① `drawPlanTab(preserveInputs=true)` 加参数，程序整体替换 `planRows` 时传 `false` 不回填；② `assignTeacherAllPlan` 改为直接改每行下拉值（不重渲染、保留其他未保存编辑）；③ `runPlanAdjust` 用 `drawPlanTab(false)`。**纪律：程序改 `planRows` 整体值后，要么直接改 DOM，要么 `drawPlanTab(false)`；手动增删行才用默认快照保留**
13. **6s 看门狗误伤编辑已修（v18→19）**：app.js 加载看门狗原在页面挂载后 6s，只要 `$el.textContent` 含"加载中"就调 `currentPageRefresh` → 整页重渲染。学科详情页静态占位"加载中"常驻隐藏容器 → 用户第一次进入课程规划正在打字/滚动时被整页重渲染打断（打字、滚动全清）。已改为：**仅当脚本压根没执行（`currentPageRefresh` 未注册）才兜底 reload 一次；活页面不再自动整页刷新**。另给 `renderPlanTab` 加"渲染前快照 + 渲染后恢复"，保证任何刷新（切回窗口/手动 ↻）都不丢未保存输入。**纪律：看门狗不再对活页面自动刷新；页面数据失败应自行渲染错误/空态**

---

## 六、当前进度

```
✅ P1 项目骨架 — 已完成
✅ P2 AI 服务层 — 已完成
✅ P3 学生档案+学科 — 已完成
✅ P4 AI对话采集 — 已完成（2026-08-02）
✅ P5 报告生成导出 — 已完成（2026-08-02）
✅ P6 成绩+规划 — 已完成（2026-08-02）
✅ P7 教师+日志 — 已完成（2026-08-08）
⬜ P8 知识库+问答
⬜ P9 工作台+打包
```
